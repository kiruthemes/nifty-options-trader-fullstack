# --- Runtime ---
NODE_ENV=development
PORT=4000
# Frontend origin (Vite default). Change to your domain in prod.
ALLOW_ORIGIN=http://localhost:5173

# --- Database (choose ONE) ---
# Local dev (SQLite)
DB_PROVIDER=sqlite
DATABASE_URL="file:./dev.db"

# Postgres (e.g., GCP Cloud SQL) — uncomment these two and comment out SQLite above
# DB_PROVIDER=postgresql
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require"

# --- Auth / Security ---
# Use a long, random string (at least 32 chars)
JWT_SECRET=iiuuhhyyggsskkiiuueerrbbhhssk

# --- Broker Defaults ---
# Used by /api/place-order when 'provider' is not sent: zerodha | dhan
DEFAULT_BROKER=zerodha

# --- Zerodha (Kite) credentials (fill when integrating) ---
ZERODHA_API_KEY=
ZERODHA_API_SECRET=
# ACCESS_TOKEN typically obtained post-login; keep rotated
ZERODHA_ACCESS_TOKEN=

# --- Dhan credentials (fill when integrating) ---
DHAN_CLIENT_ID=
DHAN_ACCESS_TOKEN=
